package heranca;

public class Teste {
    public static void main(String[] args){}
        // Animal k = new Leao();

        // k.setNome("Simba");
        // k.correr();

        // System.out.println(k instanceof Leao);

        Animal X [] = new Animal[3];
        x[0] = new Leao();
        x[0].correr();
        x[1] = new Urso();
        x[1].correr();
        x[2] = x[1]
        x[2].correr();
        if(x[0] instanceof Leao){
            Leao U = (Leao) x[0];
            U.rugir();
        }

        //saida

        //leao correndo
        //animal correndo pois urso nao tem correr, ele vai pra cima
        //animal correndo pois urso nao tem correr, ele vai pra cima
        //leao rugir

        x[3] = New Garça("Dynna");
        System.out.println([3].toString());

        //[animal] - Dynna

        public class Urso{
            publc urso(){

            }
            public Urso(String n){
                super(n);
            }
            public string toString(){
                return super.toString() + "(Urso)"
            }
        }

        //o super so pega parametro nas subclases

        this(idade) // io chama um construtor.

        ex: public animal(int idade){}
    }
}
